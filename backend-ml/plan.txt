
┌─────────────────────────────────────────────────────────────────────┐
│                   1. DATA PIPELINE (Pandas) ✅ COMPLETED           │
├─────────────────────────────────────────────────────────────────────┤
│  - استخراج البيانات من MySQL (9 جداول)                            │
│  - تنظيف البيانات: إزالة الأعمدة المكررة                          │
│  - هندسة الميزات (Feature Engineering):                            │
│    • حساب المتوسطات والاتجاهات على 30 يوماً                      │
│    • النافذة الزمنية: Features [J-37 à J-8] / Target [J-7 à J]   │
│    • استخدام NaN للبيانات المفقودة (بدلاً من 0 أو الإسناد)       │
│  - توليد بيانات اصطناعية واقعية (V10) مع:                         │
│    • وزن حسب السلالة/الجنس/العمر (σ = 16.3 كغ)                   │
│    • توزيع BCS واقعي (σ = 0.66)                                  │
│    • 35% لم يمرضوا قط / 15% لم يلقحوا قط                         │
│  - التحقق من الجودة:                                              │
│    • الارتباطات بين 0.15 - 0.35 ✅                               │
│    • لا تسرب زمني ✅                                              │
│    • توزيعات واقعية ✅                                            │
│  - الإخراج: dataset_final_v10.csv (200,000 سجل، 33 عموداً)       │
└─────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   2. MODEL TRAINING (XGBoost)       │
├─────────────────────────────────────────────────────────────────────┤
│  - تقسيم زمني (Temporal Split):                                   │
│    • Train: 70% (الأشهر الأولى)                                   │
│    • Validation: 15% (شهر متوسط)                                  │
│    • Test: 15% (الأشهر الأخيرة)                                   │
│  - المعالجة المسبقة (Preprocessing):                               │
│    • OneHotEncoder فقط للسلالة والجنس                             │
│    • ❌ NO StandardScaler (XGBoost لا يحتاجه)                     │
│    • ❌ NO Imputation (نترك NaN لـ XGBoost)                       │
│  - تدريب XGBoost:                                                 │
│    • scale_pos_weight = 11.5 (نسبة الأصحاء 92% / المرضى 8%)      │
│    • ضبط المعايير: max_depth, learning_rate, n_estimators        │
│  - تحديد العتبة المثلى (Threshold Tuning):                        │
│    • تحسين Recall أو F1-Score على مجموعة التحقق                  │
│    • العتبة المتوقعة: 0.25 - 0.35                                │
│  - تقييم الأداء:                                                  │
│    • AUC-ROC: 0.78 - 0.84 (المستهدف)                             │
│    • Recall: 0.72 - 0.78 (الأهم)                                 │
│    • Precision: 0.35 - 0.45                                      │
│    • F1-Score: 0.48 - 0.55                                       │
│  - تحليل SHAP (إلزامي):                                           │
│    • حساب SHAP values لعينة 500 من التحقق                         │
│    • استخراج أهم 5 عوامل مؤثرة لكل تنبؤ                          │
│  - الإخراج:                                                       │
│    • pipeline_final.pkl (النموذج + المعالجات)                    │
│    • model_xgboost.pkl (النموذج فقط)                             │
│    • feature_names.pkl (أسماء الميزات بالترتيب)                  │
│    • threshold_optimal.pkl (العتبة المثلى)                       │
│    • shap_explainer.pkl (مفسر SHAP)                              │
│    • confusion_matrix.png, roc_curve.png, feature_importance.png │
│    • shap_summary.png                                            │
└─────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   3. ML API (FastAPI) 🔴 À DÉVELOPPER              │
├─────────────────────────────────────────────────────────────────────┤
│  - تحميل النموذج والمعالجات (joblib.load)                        │
│  - التحقق من المدخلات (Pydantic Validation):                      │
│    • حدود واقعية لكل متغير (مأخوذة من V10)                       │
│    • bcs_last: 1.5 - 4.5                                         │
│    • temp_mean_30d: 37.5 - 41.0                                  │
│    • weight_mean_30d: 10 - 95                                    │
│    • وغيرها من المتغيرات                                         │
│  - التخزين المؤقت (Redis Caching):                                │
│    • تنبؤات فردية: TTL 6-12 ساعات                               │
│    • قائمة الحيوانات الخطرة: TTL 1 ساعة                          │
│  - نقاط النهاية (Endpoints):                                      │
│    • POST /predict/animal → تنبؤ + تفسير SHAP                    │
│    • POST /predict/batch → تنبؤ دفعات (BackgroundTasks)          │
│    • GET /predict/risky-animals → قائمة خطرة (مخزنة مؤقتاً)     │
│    • GET /health → صحة الخدمة                                    │
│  - تفسير SHAP (إلزامي في الاستجابة):                             │
│    • أهم 5 عوامل مؤثرة مع قيمها                                  │
│    • مساهمة كل عامل في التنبؤ                                    │
│  - الإخراج: خدمة FastAPI تعمل على منفذ 8000                      │
└─────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   4. BACKEND (Node.js + Hono) 🔴 À DÉVELOPPER      │
├─────────────────────────────────────────────────────────────────────┤
│  - Routes API:                                                     │
│    • GET /api/predictions/animal/:id → استدعاء FastAPI            │
│    • GET /api/predictions/risky-animals → قائمة خطرة              │
│    • POST /api/predictions/batch → تنبؤ دفعة (Admin only)         │
│    • POST /api/predictions/refresh-cache → تحديث التخزين (Admin)  │
│  - Middleware الصلاحيات:                                           │
│    • ADMIN: جميع التنبؤات، إدارة المزارع                          │
│    • VETERINAIRE: رؤية جميع التنبؤات، تحديث بيانات الحيوانات     │
│    • ELEVEUR: رؤية تنبؤات قطيعه فقط                               │
│  - تخزين التنبؤات في قاعدة البيانات:                              │
│    • جدول predictions: animal_id, prediction, probability,        │
│      risk_level, timestamp, features_json, explanations_json     │
│  - معالجة الدفعات:                                                 │
│    • تقسيم إلى مجموعات 1000 حيوان لكل طلب                        │
│    • استخدام BackgroundTasks لتجنب وقت الاستجابة الطويل          │
│  - الإخراج: واجهة برمجة متكاملة للتطبيق                          │
└─────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   5. FRONTEND (React Native / Expo) 🔴 À DÉVELOPPER│
├─────────────────────────────────────────────────────────────────────┤
│  - شاشة Dashboard:                                                 │
│    • عدد الحيوانات المعرضة للخطر (إحصائية)                       │
│    • عدد التنبيهات اليوم                                          │
│    • قائمة بأحدث 10 تنبؤات                                        │
│    • رسم بياني لتوزيع المخاطر في القطيع                          │
│  - شاشة قائمة الحيوانات الخطرة:                                   │
│    • قائمة مرتبة حسب الاحتمالية (تنازلياً)                       │
│    • مستويات خطورة ملونة (أخضر/أصفر/أحمر)                        │
│    • زر للانتقال إلى التفاصيل                                     │
│  - شاشة تفاصيل الحيوان:                                           │
│    • البيانات الأساسية (الوزن، BCS، الحرارة، النشاط)            │
│    • التنبؤ مع الاحتمالية ومستوى الخطورة                         │
│    • تفسير SHAP: أهم العوامل المؤثرة مع قيمها                    │
│    • توصيات عملية للمربي                                          │
│  - إشعارات الدفع (Push Notifications):                            │
│    • إشعار فوري عند اكتشاف حيوان عالي الخطورة                    │
│    • تقرير صباحي يومي بحالة القطيع                               │
│    • إشعار للمسؤولين عند تحديث النموذج                           │
│  - الإخراج: تطبيق جوال جاهز للاستخدام                            │
└─────────────────────────────────────────────────────────────────────┘
                                   ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   6. PRODUCTION & OPERATIONS 🔴 À PLANIFIER        │
├─────────────────────────────────────────────────────────────────────┤
│  - إعادة التدريب التلقائي:                                         │
│    • شهرياً على البيانات الجديدة                                  │
│    • عند انخفاض AUC عن 0.75 على البيانات الحديثة                 │
│  - المراقبة (Monitoring):                                          │
│    • AUC-ROC: تنبيه إذا انخفض عن 0.75                            │
│    • وقت الاستجابة: تنبيه إذا تجاوز 2 ثانية                      │
│    • عدد التنبؤات: تتبع الاستخدام اليومي                         │
│    • معدل الإنذارات الكاذبة: تتبع دقة التنبؤات                   │
│  - الإخراج: نظام جاهز للإنتاج                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 **ملخص المهام حسب الأولوية**

| الأولوية | المرحلة | المهمة | الحالة |
|----------|---------|--------|--------|
| 🔴 1 | **التدريب** | كتابة `train_final_model.py` | 🔴 **نحن هنا** |
| 🔴 2 | **التدريب** | تدريب XGBoost + ضبط المعايير | ⏳ التالي |
| 🔴 3 | **التدريب** | تحديد العتبة المثلى + SHAP | ⏳ التالي |
| 🟠 4 | **API** | كتابة `api.py` (FastAPI) | ⏳ التالي |
| 🟠 5 | **API** | إضافة Redis Caching + Pydantic | ⏳ التالي |
| 🟡 6 | **Backend** | دمج مع Hono (Routes + Middleware) | ⏳ التالي |
| 🟡 7 | **Frontend** | تطوير شاشات Dashboard + قائمة | ⏳ التالي |
| 🟢 8 | **Frontend** | شاشة التفاصيل + إشعارات | ⏳ التالي |
| 🟢 9 | **Operations** | إعادة التدريب + المراقبة | ⏳ التالي |

---

## ✅ **ما تم إنجازه حتى الآن**

- ✅ **المرحلة 1 (Data Pipeline)**: **مكتملة بالكامل** → `dataset_final_v10.csv` جاهز.
- ✅ **التحقق من جودة البيانات**: تم مع Kimi AI، النسخة V10 معتمدة.
- ✅ **جميع التصحيحات**: StandardScaler محذوف، SHAP إلزامي، تحديد العتبة، Redis، Pydantic، معالجة الدفعات.

---

## 🚀 **الخطوة التالية المباشرة**

**كتابة `train_final_model.py`** وفقاً للمواصفات التالية:

| العنصر | المواصفات |
|--------|-----------|
| **التقسيم** | زمني (70% Train, 15% Val, 15% Test) |
| **المعالجة** | OneHotEncoder فقط (لا StandardScaler) |
| **التعامل مع NaN** | تركها كما هي (XGBoost يدعمها) |
| **النموذج** | XGBoost مع `scale_pos_weight=11.5` |
| **تحديد العتبة** | تحسين Recall أو F1 على Val |
| **SHAP** | إلزامي (حساب SHAP values) |
| **المخرجات** | pipeline.pkl, threshold.pkl, shap_explainer.pkl, رسوم بيانية |

---

**هل أنت مستعد لبدء كتابة `train_final_model.py` الآن؟** 🚀🐑